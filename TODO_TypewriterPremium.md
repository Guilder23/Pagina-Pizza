# TODO_TypewriterPremium

- [ ] Ajustar `js/typewriterPremium.js` para que el texto **PIZZAS PREMIUN** se escriba desde cero en **cada** recarga/refresh.
- [ ] Eliminar o ignorar la lógica de `sessionStorage` que impide que se vuelva a animar.
- [ ] Verificar que el resultado final quede siempre como `PIZZAS PREMIUN` aunque haya cambios de pestaña/recarga.

